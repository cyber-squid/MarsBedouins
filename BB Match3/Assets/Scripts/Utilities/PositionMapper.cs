using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
/*
public class PositionMapper : MonoBehaviour
{
    Vector3[,] posMap = new Vector3[GridGenerator.boardSize, GridGenerator.boardSize];
    PositionMapper Instance = new PositionMapper();
    public void SetMap(int[] coord, Vector3 pos)
    {
        posMap[coord[0], coord[1]] = pos;
    }
    public Vector3 Convert(int[] coord)
    {
        return posMap[coord[0], coord[1]];
    }
}
*/